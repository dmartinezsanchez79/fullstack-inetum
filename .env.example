# Copia este fichero a `.env` y cambia SECRET_KEY por un valor seguro
SECRET_KEY=change_me_to_a_random_long_secret_value
# Opcional: ajustar orígenes CORS separados por comas (ej: http://localhost:3000)
# CORS_ORIGINS=http://localhost:3000,https://example.com
